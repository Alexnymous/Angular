import { SurvolActiveDirective } from './survol-active.directive';

describe('SurvolActiveDirective', () => {
  it('should create an instance', () => {
    const directive = new SurvolActiveDirective();
    expect(directive).toBeTruthy();
  });
});
